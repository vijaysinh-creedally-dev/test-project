name: CI

on:
  push:
    branches: [develop, staging, main]
  pull_request:
    branches: [develop, staging, main]

jobs:
  quality:
    uses: vijaysinh-creedally-dev/.github/.github/workflows/reusable-lint-test.yml@main
